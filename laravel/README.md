# OB Entry Book — Laravel rebuild

This directory contains the Laravel rebuild of the Digital Security Occurrence Book.

The legacy application under `../Entry Book/` remains untouched, including its PHP, JavaScript, CSS, and XML data files.

Initial milestone: establish a minimal Laravel application skeleton before migrating occurrence entries, management instructions, authentication, acknowledgements, and XML import logic.
